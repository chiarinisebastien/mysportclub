<form method="post" action="{{ path('app_training_delete', {'id': training.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ training.id) }}">
    <button class="btn btn-danger btn-sm col-lg-1 col-sm-12 col-md-12 mt-2 mb-2 ms-2">Delete</button>
</form>

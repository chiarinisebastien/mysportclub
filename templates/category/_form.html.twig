{{ form_start(form) }}

<div class="col-12">
    <div class="row">
        <div class="col-lg-12 col-sm-12 col-md-6">{{ form_row(form.title) }}</div>
    </div>

    <div class="row">
        <div class="col-lg-12 col-sm-12 col-md-12">{{ form_row(form.submit) }}</div>
    </div>
</div>

{{ form_end(form) }}
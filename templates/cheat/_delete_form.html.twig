<form method="post" action="{{ path('app_cheat_delete', {'id': cheat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cheat.id) }}">
    <button class="btn">Delete</button>
</form>

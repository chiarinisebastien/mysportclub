{{ form_start(form) }}

<div class="col-12">
    <div class="row">
        <div class="col-lg-3 col-sm-12 col-md-6">{{ form_row(form.firstname) }}</div>
        <div class="col-lg-3 col-sm-12 col-md-6">{{ form_row(form.lastname) }}</div>
    </div>
</div>

<div class="row">
    <div class="col-lg-12 col-sm-12 col-md-12">{{ form_row(form.submit) }}</div>
</div>

{{ form_end(form) }}